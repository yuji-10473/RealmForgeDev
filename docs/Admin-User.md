# 権限管理およびユーザーフロー 仕様書 (Admin & User Flow Specification)

## 1. 概要
本アプリケーションでは、ユーザーの権限を「管理者」と「一般ユーザー」の2段階に分け、利用可能な機能を制限します。また、一般ユーザー（テスター）が迷うことなくスムーズにゲームを体験できる専用のログインフローを提供します。

## 2. 権限の定義
- **管理者 (Admin)**: 全ての編集機能（マップ、キャラクター、イベント等）およびプレイテストを利用可能。
- **一般ユーザー (User)**: プレイテスト機能のみを利用可能。ログイン後は専用のプレイテスト画面へ誘導される。

## 3. データ構造 (Firestore)
権限の判定は、Firestoreの以下のパスにドキュメントが存在するかどうかで行います。

- **コレクションパス**: `/admins/{userId}`
- **ドキュメント内容**:
  ```json
  {
    "isAdmin": true
  }
  ```
※ `userId` は Firebase Authentication の UID と一致する必要があります。

## 4. セキュリティルール
`firestore.rules` にて、ユーザーが自分自身の管理者情報を読み取れるように設定されています。
```rules
match /admins/{userId} {
  allow read: if isOwner(userId);
}
```

## 5. UI/UX の制限ロジック
共通レイアウト（`layout.tsx`）において以下の制御を実装します。
- **権限チェック**: ログインユーザーの UID に基づき、`/admins/{userId}` ドキュメントの存在を監視。
- **サイドバーの動的表示**: 
    - 管理者の場合：全てのメニューを表示。
    - 一般ユーザーの場合：「プレイテスト」および「ログイン/ログアウト」のみを表示。
- **アクセスガード**: 一般ユーザーが直接エディター画面を開いた場合、自動的に `/play-test` へリダイレクトする。

## 6. 管理者の追加方法
Firebase Console から手動で設定します。
1. `admins` コレクションに、管理者にしたいユーザーの `UID` をドキュメントIDとして作成。
2. フィールド `isAdmin` (boolean) を `true` に設定。

## 7. ユーザー体験とログインフロー (User Experience & Login Flow)

一般ユーザー（プレイテストユーザー）が最初に目にする画面から、プレイ開始までのフローを定義します。

### 7.1. ランディング・ログイン画面 (Landing/Login Page)
ログインしていないユーザーが最初にアクセスする画面です。
- **ビジュアル**: RealmForge の世界観を伝える背景画像やロゴを配置し、期待感を高めるデザイン。
- **情報提供**: 「このアプリで何ができるか」の簡潔な説明。
- **利用規約 (ToS)**: ログインボタンの近くに、利用規約やプライバシーポリシーへのリンクを表示。
- **アクション**: 目立つ「Googleでログインしてプレイを開始」ボタンを中央に配置。

### 7.2. ログイン後の遷移 (Post-Login Redirection)
- **自動遷移**: ログイン完了後、ユーザーの権限を確認。
- **一般ユーザー**: 即座に `/play-test` ページへリダイレクト。
- **管理者**: 最後に開いていた、もしくはデフォルトの「マップエディター」を表示。

### 7.3. プレイテスト開始画面 (Play-Test Load Screen)
`/play-test` に到達した際、即座にゲームが始まるのではなく、以下の「ロード画面」を表示します。
- **ロード (Load)**:
    - Firestore の `playtestSaves/{userId}` にデータが存在する場合のみ活性化。
    - 最終保存日時をボタン付近に表示。
    - クリックで保存された座標・マップ・所持品を復元して開始。

### 7.4. UIのクリーンアップ
一般ユーザーモードでは、開発用のツールバーや設定デバッグ情報を非表示にし、ゲーム体験に没入できるようにします。
